
// Terminal Interface
$ sudo apt install lm-sensors
$ sudo sensors-detect
$ sensors


// Graphical Temp Tracking
$ sudo apt install psensor


// Graphical Sys Info/Benchmarking
$ sudo apt install hardinfo

// Sysbench
$ sudo apt install sysbench
$ sysbench cpu run
$ sysbench memory run
$ sysbench --help
